CSS:
------
->css stand for cscading style sheets used to apply stylings, layouts , animation to web pages
->css is introduced in the year 1994 by the developer "hakon wium lie"
->the present version of css is CSS3 

Methods to apply stylings to web page:
---------------------------------------
  1.inline CSS/ INLINE stylings
   -> applying styles by taking 'style atribute' within the elements.
   ->this style are applicable for only that elements
   -> for inline css not required CSS selectors
   ->inline styling is more priority then the internal & external
  2.internal CSS/internal stylings
   ->applying stylings by taking <style></style> tag in head part is called as a internal CSS/ internal stylings
   ->to apply internal CSS we need CSS selectors 
  3.external styling CSS/ extenal styling 
   ->apply styling by taking new.css file and link to html file by using <link></link>
   ->

CSS selectors:
-----------------
 -> css selectors are used to target HTML elements tp apply styling 
 -> css selectors are present only in internal and external css 
 ->to apply external CSS we need css selectors

 types of CSS selectors:
 -----------------------
  1.simple selectors
  2.combinator selectors 
  3.pseudo class selectors
  4.pseudo elements selectors
  5.attribute selectors

  simple selector:
    1.ID selector (#)
    2.class selector (.)
    3.tag selector 
    4.grouping selector (,)
    5universal selector( *)

  combinator selector:
   1.descent selectors (space)
   2.child selector(>)
   3.adjacent sibling selector(+)
   4.general sibling selectors(~)  

  pseudoclass selectors:
   1.structural pseudo class selectors
    1. :first-child
    2. :last-child
    3. :first-of-type
    4. :last-of-type
    5. :nth-child()
   2.dynamic psuedo class selectors
    1. :active      ---|
    2. :visited     ---|->anchor tag
    3. :focus       --- ->input tag
    4. :hover

  4.psuedo element selector:(can apply either internal or external styling)
   1 ::after
   2 ::before
   3 ::first-letter
   4 ::first-line
   5 ::maker  (only applicable for lists)
   6 ::slection (applying stylings during selections)(only color and background-color are applicable)
   7 ::placeholder  
 
   Attribute selectors:
   ---------------------
     ->attribute selctors are used to target HTML element based on attributes.
     ex: <h1 align="center"  id="heading"> HELLO </h1>
     <h1  id="heading"> HELLO </h1>

        syntax -> selector[attributename="value"]{......styling.....}
     
      types:
        1.[attributename="value"]
        2.[attributename^="value"] (targets first letter/value)
        3.[attributename$="value"] (targets last value)
        4.[attributename|="value"] (used to target the starting point which are in between the hyphen - ex:heading-tag style is applicable for heading and heading tag)
        5.[attributename*="value"]
 
 color values:
 --------------
   1.color name
   2.rgb(red(0-255),green(0-255),blue(0-255))
   3.rgba(red, green, blue,alpha)(alpha should be in the range 0-1)
   4.hsl(hue(0-360),saturation(1-100%),lighteness(1-100%))
   5.hsla(hue,saturation,lightness,alpha)
   6.hexa code()
   7.transparent
   8.color()

 text properties:
------------------
   ->text properties are used to apply stylings for 'text'
  properties:
  ------------
  1.text-alignments
     i.text-align: left, right, center, start, end, justify.
     ii.word-spacing
     iii.letter-spacing
     iv.text-indent
     v.line-height
  2.text decoration properties
    i.text-decoration-line: none,underline, overline, line-through
    ii.text-decoration-color
    iii.text-decoration-style: solid, double, dotted, dashed, wavy, none.
    iv.text-decoration-thickness
    v.text-decoration (short hand form)
  3.text transform properties:
    i. text-transform : lower case , uppercase , capitalized

  4.text-overflow properties:
   i.overflow       :visible , auto , hidden , scroll
   ii.overflow-x
   iii.overflow-y
  5.font properties
   i.font-size
   ii.font-style
   iii.font-weight
   iv.font-family

css BOX model:
--------------
  1.padding
    ->the space between content and border 
     padding properties:
       1.padding-top
       2.padding-bottom
       3.padding-left
       4.padding-right
       5.padding(short hand form)


  2.border
    ->the space between margin and padding
       border properties
       ---------------
         1.border-style : none,solid,
         2.border-weight
         3.border-color
         4.border(short and form)

         border radius properties:
         -------
         1.border-top-left-radius
         2.border-top-right-radius
         3.border-bottom-left-radius
         4.border-bottom-right-radius
         5.border-radius-shf
  3.margin
      1.margin-top
      2.margin-bottom
      3.margin-left
      4.margin-right
      5.margin(short hand form)

flex properties:
    1. display :flex;
    2.flex-direction (row-reverse, coloumn,)
    3.flex-wrap
    4. justify-content  (right, left, center, space-between, space-around,)
    5.align-items (by default it shows baseline means top) (baseline, center, end)
    6. flex-grow
    7. flex-shrink
    8. flex


    grid properties:
      -> used to display content in a table form
     1.display :grid
     2.grid-template-columns
     3.grid-template-rows
     4.row-gap
     4.column-gap
     5.grid-row-start
     6.grid-row-end
     7.grid-column-start
     8.grid-row-end



    position properties:
        1.position: fixed, relative, absolute, static, sticky, left, right
    animations:
       1.define animation using @keyframes
       2.link that animation to html element
       3.we can use "from" "to" or 0% to 100% in between the @keyframe styling.

  media queries:
      -> these are used to build responsive web pages based on device width(device view port)
      -> brreak points
        1.smaller devices (mobiles with brands)   0px-450px
        2.medium devices (tablets / iPads)        450px-850px
        3.large devices (laptops)                 850px-1200px
        4.X large devices (digital screenbs)      >1200px

    ->@media screen  (min-width, max-width)


 









git init  
git add .
git commit -m "name"
git branch -M  main
push origin main
